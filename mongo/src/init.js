// Entrando no bando de dados admin e realizando autenticação. Após isso, várias operações são permitidas.

// Copiar e colar no terminal o código abaixo após entrar no console do Mongo.

use admin
db.auth('mongo_humongous', 'mongo_1234')
